package com.example.checkitems.ui.view_model

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class ItemViewModel(application: Application) : AndroidViewModel(application) {
}